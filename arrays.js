var chocolateBars = ["snickers", "hundred grand", "kitkat", "skittles"];

function addElementToBeginningOfArray(array, element) {
  const array = ["Hi", "Hello", "Hola"]
  const modifiedArray = ["element", ...array]
  return modifiedArray
}